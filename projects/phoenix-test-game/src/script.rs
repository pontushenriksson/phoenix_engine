pub mod game {
  // Example stuff
  // use bindings;
  
  /*

  enum PhoenixGameState {
    Good,
    Bad,
    Ok,
  }

  */

  pub fn update() -> PhoenixGameState {
    // Example
    // let player: KinematicBody3D = bindings::make_kinematic_body3D();

    // player.move(bindings::KeyPolling::KeyEvent());

    Ok
  }
}
